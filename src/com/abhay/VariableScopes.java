package com.abhay;

public class VariableScopes {
    public static void main(String[]args){
        //local variables are variables that are created an can only be accessed inside that method.
        // == they are local to that method

        //global variables == are created outside of a methods and are visible to all parts of the class.
        DiceRoller roller = new DiceRoller();

    }
}
